{
  "name": "gateway-service",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "npm run build && node ./build/gateway/gateway.js",
    "build": "tsc",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "@types/socket.io": "^2.1.4",
    "typescript": "^3.7.5"
  }
}
